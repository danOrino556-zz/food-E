
import { toArray } from 'food-e/helpers/to-array';
import { module, test } from 'qunit';

module('Unit | Helper | to array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toArray([42]);
  assert.ok(result);
});

